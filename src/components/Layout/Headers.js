const { Fragment } = require("react");

const Header = () => {
  return (
    <Fragment>
      <header>
        <h2>The Meals App</h2>
        <button>Cart - 3</button>
      </header>
    </Fragment>
  );
};

export default Header;
